#!/bin/sh

bash -s - "$@" < "$(command -v wizard)"

# tmp="$(mktemp /dev/shm/wizard.XXXXXXXXXXXXXXXXXXXXXX)"
# cp "$(command -v wizard)" "$tmp"
#
# bash "$tmp" "$@"
# ret=$?
#
# rm -f "$tmp"
# exit "$ret"
