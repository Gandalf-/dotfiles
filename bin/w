#!/bin/sh

if [ -e /dev/shm ]; then
  tmp="/dev/shm"
else
  tmp="/tmp"
fi

tmp="$(mktemp "$tmp"/wizard.XXXXXXXXXXXXXXXXXXXXXX)"
cp "$(command -v wizard)" "$tmp"

bash "$tmp" "$@"
ret=$?

rm -f "$tmp"
exit "$ret"
